name: pioneers_smartsave
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.63.0  # Added charting library
  provider: ^6.1.0  # State management
  firebase_core: ^2.15.0  # Firebase integration
  firebase_auth: ^4.6.0  # Firebase authentication
  cloud_firestore: ^4.8.0  # Firestore database
  http: ^0.13.6  # HTTP requests
  shared_preferences: ^2.2.0  # Local storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true